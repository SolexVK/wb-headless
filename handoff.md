# Handoff — Аналитика Wildberries (ниша + топ товаров + артикулы)

## Цель
Набор инструментов оценки WB по данным MPStats:
1. **Анализ ниши/категории** — скоринг 0–100, вердикт, реализация, экономика, Оракул, PDF. (в main)
2. **Топ-N товаров** — лидеры категории по выручке: фото, ссылки, бренд, остатки/склады, динамика,
   доп. колонки (выкуп/возраст/размеры/реклама). PDF + интерактивный HTML.
3. **Годовая аналитика по артикулам (SKU)** — по конкретным товарам: выручка, «заработал»
   (= выручка × выкуп%), упущенная выручка, помесячная динамика, фото ×3, кликабельные арт./бренд.
Все три — на любой категории/фильтре/наборе SKU (CLI). Данные — только через MPStats.

## Текущее состояние
Всё работает на уровне кода. Рабочая ветка `claude/niche-analysis-q6jw7e` (= отслеживает origin/main
после мержа PR #12; PR под товарные отчёты НЕ заводили — по решению пользователя).
Коммиты этой сессии (сверху вниз):
- `d273ae9` feat(sku): годовой отчёт по артикулам — выручка/заработал/упущенка
- `8502111` feat(top): доп. колонки — выкуп, возраст, размеры, реклама
- `fc2ce59` docs: update handoff  · `bd1f7fc` feat(top): отчёт «Топ-N товаров»
- `6e34e81` (влитый PR #12 — весь анализ ниши)

**⚠ ГЛАВНЫЙ БЛОКЕР: суточный лимит MPStats API исчерпан (HTTP 429, «за 19.08.2026»).** Сегодня
(19.08.2026, ~18:00 МСК) НИ ОДИН запрос к API не проходит — проверял 3+ раза (категория, item/full,
item/sales). Живьём собрать SKU-отчёт по артикулам пользователя нельзя до сброса лимита.
Сброс ориентировочно **20.08 в 00:05 МСК** (21:05 UTC), либо на более высоком тарифе.

`MPSTATS_TOKEN` — в окружении контейнера (len 55, сам токен рабочий, дело именно в квоте).
Отчёты пишутся в `reports-output/` (в .gitignore). Вёрстка всех трёх отчётов проверена мок-рендерами.

## Файлы, над которыми работали
- `lib/mpstats.js` — добавлен экспорт `fetchItemFull(sku,d1,d2)` (`/wb/get/item/{sku}/full` с
  period_stats). (Ранее в сессии: normalizeCategoryRow дополнен thumb/url/graphs/warehouse-полями.)
- `lib/topProducts.js` — проброс доп. полей в товар: buyoutPct, firstDate, ageDays, hasAd, searchCpm,
  sizeCount, sizeInStock + хелпер `daysSince`.
- `lib/topProductsHtml.js` — мета-строка в карточке (выкуп%/возраст/размеры/реклама) + хелперы
  `fmtAge`/`adLabel` + CSS `.metarow`. Выкуп <70% — красным.
- `lib/skuReport.js` — НОВЫЙ. `buildSkuReport({skus,d1,d2})` + `parseSku` (арт. из ссылки WB).
  Защитное чтение period_stats по нескольким ключам (revenue/lost_profit/purchase/sales), фолбэк
  выручки на сумму дневного ряда, «заработал»=revenue×buyout%, помесячная агрегация выручки, фото data-URI.
- `lib/skuReportHtml.js` — НОВЫЙ. `buildSkuReportHtml` — 3 денежные плитки (выручка/заработал/упущенка),
  фото ×3 hover, кликабельные арт.+бренд, помесячные столбцы. Акцентная схема, без залезаний.
- `report-sku.js` — НОВЫЙ. CLI: `node report-sku.js <арт|ссылка ...>`, env SKU_D1/SKU_D2/SKU_TITLE/SKU_PDF.
- `report-top.js`, `lib/topProducts*.js` — товарный отчёт (прошлая сессия), доп. колонки — эта.
- `handoff.md` — этот файл.

## Что изменилось
- Топ-отчёт получил мета-строку доп. колонок (все поля уже приходят из /wb/get/category — доп.
  запросов не нужно). Крайние случаи: низкий выкуп→красный, пустой размерный ряд→«—», возраст дн/мес/годы.
- Добавлен ТРЕТИЙ тип отчёта — по артикулам (SKU). Источник: `/wb/get/item/{sku}/full` (period_stats:
  выручка/упущенка/выкуп/бренд/фото/ссылка) + `/wb/get/item/{sku}/sales` (дневной ряд → помесячная
  выручка и фолбэк суммы). «Заработал» = выручка × выкуп% (по выбору пользователя).
- Артикулы пользователя: **881535653, 188207534, 806472155**, период — последний год.

## Что пробовали и НЕ сработало
- **API MPStats: 429 весь день.** Пробовал `/wb/get/category`, `/wb/get/item/{sku}/full`,
  `/wb/get/item/{sku}/sales` — все `429 «Превышен лимит запросов за 19.08.2026»`. Это КВОТА ТАРИФА, не
  код (токен рабочий, раньше в сессии те же эндпоинты давали 200). Вывод: живая сверка полей
  period_stats и генерация SKU-отчёта — только после сброса лимита. Structure period_stats НЕ
  подтверждена живьём → в skuReport.js читаю поля защитно (несколько ключей-кандидатов), при первом
  живом прогоне свериться и при нужде поправить имена ключей.
- **Дневной ряд НЕ содержит упущенку.** `normalizeDailyRow` (mpstats.js:118) даёт date/balance/sales/
  price/revenue — БЕЗ lost_profit и без выкупа. Поэтому годовую выручку можно суммировать из /sales,
  но упущенку и выкуп берём из /full period_stats (или из строки категории). Обошёл: основной источник
  — /full, фолбэк выручки — сумма /sales.
- **Эндпоинты по складам не существуют** (из прошлой сессии, подтверждено): item/{sku}/by_warehouses,
  /warehouses, /stocks, /by_region — все 405. Гео-разбивки по складам в API нет.
- **Планировщик автопрогона — пользователь прервал** вызов send_later (mcp Claude_Code_Remote). Значит
  автозапуск НЕ поставлен. Ждём явного решения: «запланируй» или «прогоню сам».
- **Формат поля `photo` в /full неизвестен** (не удалось проверить из-за 429). В skuReport.js
  `photoCandidates` читает строку/массив/объект + thumb_middle. При первом живом прогоне проверить, что
  фото реально скачивается; если нет — взять thumb из строки категории или собрать URL по nmId.
- **Мок-рендер вместо живого.** Т.к. API недоступно, вёрстку обоих новых кусков (доп. колонки топа и
  весь SKU-отчёт) проверял синтетическими данными через buildXxxHtml + screenshot хромиумом. Это
  проверяет ТОЛЬКО вёрстку, не значения из API.

## Команды для проверки
```bash
cd /home/user/wb-headless
# 1. Синтаксис всех модулей (formal-тестов НЕТ)
for f in lib/mpstats.js lib/nicheAnalysis.js lib/nicheReport.js lib/nicheReportHtml.js \
  lib/oraclePhrases.js lib/renderPdf.js lib/topProducts.js lib/topProductsHtml.js \
  lib/skuReport.js lib/skuReportHtml.js report-niche.js report-top.js report-sku.js \
  server.js scripts/wb-category.mjs scripts/oracle-phrases.mjs; do node --check "$f" || echo "FAIL: $f"; done
# ждём: без FAIL

# 2. Живой SKU-отчёт (ТРЕБУЕТ сброшенного лимита API!):
node report-sku.js 881535653 188207534 806472155
# → reports-output/sku-881535653-188207534-806472155-<d1>_<d2>.{pdf,html,json}
#   в консоли по каждому: выручка/заработал/упущено/выкуп; при ⚠ «/full не получена» — снова лимит/арт.
#   ПРИ ПЕРВОМ живом прогоне: свериться, что revenue/lostProfit/buyoutPct не нули (иначе поправить
#   ключи чтения period_stats в lib/skuReport.js pick(...)), и что фото скачалось (hasImg=true в json).

# 3. Живой топ-отчёт с доп. колонками (ТРЕБУЕТ лимита):
TOP_PLUS="полос" TOP_MINUS="коротк" TOP_N=10 \
  node report-top.js "Мужчинам/Рубашки" 2026-07-15 2026-08-13 "Топ-10 рубашек в полоску"

# 4. Быстрая проверка, сбросился ли лимит (1 запрос):
node -e 'const t=process.env.MPSTATS_TOKEN;fetch("https://mpstats.io/api/wb/get/item/881535653/full?d1=2025-08-19&d2=2026-08-18",{headers:{"X-Mpstats-TOKEN":t}}).then(r=>r.text()).then(x=>console.log(x.slice(0,120)))'
# 200/JSON → лимит сброшен, можно гнать; 429 → ещё держится

# 5. Рендер HTML→PNG для визуальной проверки вёрстки:
/opt/pw-browsers/chromium --headless --no-sandbox --disable-gpu --hide-scrollbars \
  --window-size=940,1500 --screenshot="out.png" "file://$(pwd)/reports-output/<файл>.html"
```
**Ожидаемое состояние (сейчас):** `node --check` по всем 16 файлам — без FAIL (ЗЕЛЁНОЕ). Живые прогоны
(#2,#3) СЕЙЧАС падают на 429 — это ВНЕШНЯЯ КВОТА, а не регресс кода; станут зелёными после сброса
лимита. Вёрстка всех отчётов проверена мок-рендерами. Красных проверок по коду нет.

## Открытые вопросы (нужно решение пользователя)
- **SKU-отчёт: как запустить живьём.** API под лимитом. Варианты: (а) я ставлю автопрогон на ~00:10
  МСК 20.08 (send_later) и присылаю готовый PDF; (б) пользователь пишет «прогони», когда лимит вернётся;
  (в) более высокий тариф MPStats. Пользователь ПРЕРВАЛ постановку автопрогона — решение отложено.
  Рекомендация: (а). ЖДУ ОТВЕТА.
- **Доработки SKU-отчёта** (по желанию): порядок плиток, добавить остаток/оборачиваемость, убрать/
  оставить помесячный график. Не блокер.
- **PR под товарные отчёты (top + sku).** Код в ветке поверх main, PR нет (пользователь: «пока не
  нужно»). Когда захочет — заведём. ЖДУ по случаю.

## Следующий шаг
Первое действие при возобновлении — проверить, сброшен ли лимит (команда #4). Если 200 — сразу
прогнать SKU-отчёт (команда #2) по артикулам 881535653/188207534/806472155, СВЕРИТЬ значения
period_stats (не нули ли revenue/lost/buyout) и что фото скачались, при нужде поправить ключи в
`lib/skuReport.js`, затем отправить PDF+HTML пользователю. Если 429 — сообщить, что лимит ещё держится,
и предложить снова (а)/(б)/(в) из открытых вопросов.
