# Handoff

## Цель
Инструмент планирования производства одежды под план продаж WB (папка `planner/`): Гант,
партии по цехам, заказ ткани, юнит-экономика, RBAC, контроль сроков и **«Ранг сезонности»**
(прогноз продаж по конкурентам). Ветка `claude/production-plan-twv8ki`, деплой на Mac mini
(служба launchd `com.wbheadless.planner`, репозиторий `/Users/openclaw/wb-headless`).

## Текущее состояние
Всё закоммичено и запушено в `origin/claude/production-plan-twv8ki`, дерево чистое (последний
код-коммит — `052aaa2`, проекция роста рынка; далее только handoff-и). Проверок красных нет.

**«Ранг сезонности» (сезонный режим) — ЗАКРЫТО и принято пользователем.** На serp-v2 (1–3
запроса/прогон), поведенческая релевантность, объём «Выкупов» = факт продаж ТОП-1/ТОП-3 за
аналогичный сезон × рост рынка. Детали — раздел «Сезонный режим» ниже.

**СЕЙЧАС В РАБОТЕ (спроектировано, согласовано, КОД НЕ НАЧАТ): межсезонная / круглогодичная
модель.** Пользователь подтвердил идею и детали; я расписал точный план правок и жду финального
«поехали» на шаг 1 (движок). Это следующий шаг — см. ниже.

### Согласованный план: круглогодичный режим («allseason»)
Задача: рядом с чисто сезонным товаром (лёгкий муслин/марлёвка — разгон-с-нуля→сезон→
распродажа-в-ноль) поддержать **круглогодичные товары с яркими сезонами** (полоска/клетка):
после сезона продажи не обнуляются, а мягко снижаются до межсезонной базы и идут весь год.

**Анализ рынка (офлайн, фикстура муслина) — доказано:**
- Межсезонье живое: провал ~20% от пика, в декабре всплеск (НГ). База прибыльна.
- Концентрация ВНЕ сезона выше (ТОП-3 17%→24%, ТОП-10 32%→39%) — монополизация есть.
- **НО сезонные ТОПы межсезонье НЕ удерживают** (3 из 10 остаются, держат 20%). Межсезонье
  выигрывает ДРУГОЙ, круглогодичный ассортимент (в срезе — пижамы: №6 в сезон → №1 вне сезона,
  21401 шт против 3135 у летнего лидера).
- Авто-подсказка типа РАБОТАЕТ по амплитуде пик/медиана+«пол»: муслин ×7.3 пол14%=сезонный,
  пижама ×1.8 пол54%=круглогодичный. Это по сути наш же «Ранг сезонности».

**План правок (поэтапно, с офлайн-проверкой после каждого шага — лимит MPStats не жечь):**
1. **Движок (`forecast.js`)** — `plan.articleType`: 'seasonal' (как есть) | 'allseason' (нов.):
   окно = 365 дн вперёд от сегодня; форма = полный годовой рельеф рынка (тот же `A`) БЕЗ
   разгона-с-нуля и БЕЗ обнуления; детектор МИНИ-СЕЗОНОВ (вторичные пики в рельефе + календарь-
   страховка НГ/14фев/23фев/8мар), каждый с разгоном до и возвратом после. Поставки:
   ровный помесячный подсорт (спрос след. месяца по рельефу + буфер, склад ~месяц вперёд, НЕ
   обнуляем) + пред-настрел перед мини-сезонами; главный сезон переходит в базу, а не в ноль.
2. **Отбор/якорь (`seasonPlanReport.js`)** — лидеры по ГОДОВЫМ продажам (уже есть
   `revenueLY`/`unitsSoldLY`); якорь `seasonTargets` авто-адаптируется (окно=год → объём=годовые
   продажи ТОП-1/ТОП-3 × рост); добавить `seasonalityHint` (амплитуда→тип) в отчёт.
3. **`seasonApi.js` + `app.js`** — проброс `cfg.articleType`; переключатель Сезонный/Круглогодичный
   + бейдж-подсказка; на графике метки мини-сезонов и линия межсезонного пола; помесячный план.

**Решения, принятые по ходу (пользователь одобрил):** старт годового окна — от сегодня на 12
мес; главный сезон = самый крупный блок, мини = вторичные пики > ~1.3× межсезонного пола;
календарь всплесков — базовый RU-набор (НГ, 14/23 фев, 8 мар), данные рынка их и так частично
ловят; подсорт — на месяц вперёд, пока шьём очередной период, С учётом помесячных колебаний И
вторичных всплесков (в объёмах И сроках — как мини-сезоны).

### Сезонный режим (serp-v2) — как устроено (готово)
- Источник — `POST /api/seo/keywords/<фраза>/serp-v2` (`planner/lib/season/wbSerp.js`): 1 запрос
  на фразу → до 200 товаров, дневные графики за 2 года. Кэш `wb_serp`, окна режутся в памяти
  (`_serpAll`). Лимит MPStats 150/день, бюджет `mpstatsBudgetToday/Add`, статус `/api/season/status`.
- Отбор: нишевый ключ по 4-символьному корню; `nicheWords`; минус-слова по стему; медианная цена
  за год; пол из карточки; «Предв. выбор» ТОП-30 без ключа (`approvedIds`); ТОП-15 с оборачив.
- Прогноз «Выкупов» (4 правки прошлой части сессии): форма-кривая (`forecast.js`); уровень
  ТОП-3/ТОП-1 = скорость лидеров по выручке ÷ активные дни за год (`activeDaysLY`); объём = факт
  продаж лидеров за аналог. сезон (`seasonTargets`); проекция роста рынка (`plan.growthMode`
  'market'|'none', зажат [0.5,2.0]).

## Файлы, над которыми работали (эта сессия)
- `planner/lib/season/seasonPlanReport.js` — `activeDaysLY`; уровень по лидерам выручки; блок
  `seasonTargets` (якорь по аналог. периоду + проекция роста рынка).
- `planner/lib/seasonApi.js` — проброс `growthMode`/`growthYears`.
- `planner/public/app.js` — `seasonVolumeBasis` (карточка «Выкупы»); оборачиваемость из balance;
  `APP_BUILD='season-serp-2026-07-28p'`.
- `handoff.md` — этот файл.
- (для межсезонья код ещё НЕ трогали — только анализ фикстуры и проектирование.)

## Что изменилось (смысл)
- «Выкупы» больше не сумма синтетической кривой по пику (была ~2× переоценка), а реальный объём
  продаж лидеров за аналогичный сезон × измеренный рост рынка. Оборачиваемость в ТОП-15 починена.
- Спроектирован (не закодирован) круглогодичный режим: та же машина, но годовое окно, форма без
  обнуления, мини-сезоны, поддерживающий подсорт.

## Что пробовали и НЕ сработало (грабли — читать!)
- **Жгли суточный лимит MPStats тест-скриптами** — пользователь злился дважды. НИКАКИХ живых
  запросов при разработке. Только офлайн через `SERP_FIXTURE_FILE=<scratchpad>/srt.json` (реальная
  выдача serp-v2 по муслину, 200 товаров × 730 дней).
- **AskUserQuestion дважды перебил пользователя** (не дал написать в «Other») — он раздражался.
  Для развилок в этой теме — задавать вопросы ТЕКСТОМ, дать ответить словами, не меню.
- **Ценовой фильтр 0 в тесте режет всё** — в движке `priceMin/priceMax=0`=«нет ограничения», но
  буквальный `priceMax:0` в тесте фильтрует всё (>0). В тестах передавай `null`, не `0`.
- **Скорость «за активный день» пускала мусор в ТОП-3** — товар, живший 2 мес, ложно попадал в
  тройку. Исправлено: ТОП-3/ТОП-1 = крупнейшие ПО ВЫРУЧКЕ, и уже их скорость.
- **Рост рынка ≠ рост карточки** — весь рынок ×1.79 (раздут новичками), когорта/лидеры ×1.25–1.37.
  Пользователь выбрал агрессивный «весь рынок». `growthMode` оставлен переключаемым.
- **ВАЖНО для межсезонья: интуиция «удержать летний трон» неверна** — сезонные ТОПы межсезонье
  не держат (данные выше). Межсезонный отбор — по годовым/межсезонным продажам, не по сезонным.
- serp endRow > 200 → HTTP 422; 404 на Mac mini даёт пустой toast (не рестартнули службу) — после
  pull ВСЕГДА `launchctl kickstart`; (Производство 2.0) мощность пошива 1 = «бесконечные» партии;
  новые top-level поля state — не забыть в `permissions.js applyWritePolicy`.

## Команды для проверки
Формальных тестов НЕТ. Из корня репо (`/home/user/wb-headless`):
```bash
# синтаксис
for f in planner/lib/season/wbSerp.js planner/lib/season/seasonPlanReport.js \
  planner/lib/season/forecast.js planner/lib/seasonApi.js planner/public/app.js; do
  node --check $f || echo "FAIL $f"; done

# ОФЛАЙН-прогон сезонного режима на фикстуре (без сети/лимита)
cd planner && SERP_FIXTURE_FILE=/tmp/claude-0/-home-user-wb-headless/d88ae761-a0a2-57e5-8cbd-9c63b9433eb7/scratchpad/srt.json \
node --experimental-sqlite --input-type=module -e '
import { buildSeasonPlanReport } from "./lib/season/seasonPlanReport.js";
const subject={path:null,limit:50,serp:{phrases:["рубашка муслиновая женская"],minusWords:[],nicheWords:["муслин"],limit:50}};
const rep=await buildSeasonPlanReport({d1:"2024-07-28",d2:"2026-07-27",subject,forecast:{targetYear:2026},baseSource:"market",plan:{oos:true,targetLevel:"top3",growthMode:"market",asOf:"2026-07-28"}});
const t=Math.round((rep.plan.forecastDaily||[]).reduce((s,d)=>s+(+d.plannedOrders||0),0));
console.log("Выкупы ТОП-3:",t,"| seasonTargets:",JSON.stringify(rep.plan.levelInfo.seasonTargets));'

# движок производства (сид)
node -e "Promise.all([import('./planner/lib/model.js'),import('./planner/lib/scheduler.js')]).then(([m,s])=>{const r=s.buildSchedule(m.normalizeState(m.defaultState()));console.log('циклов',r.cycles.length,'срывов',r.warnings.filter(w=>w.level==='error').length);})"
```
Деплой на Mac mini: `git pull origin claude/production-plan-twv8ki` → `launchctl kickstart -k
gui/$(id -u)/com.wbheadless.planner` → в браузере Cmd+Shift+R.

**Ожидаемое состояние (зелёное сейчас):** все `node --check` проходят; офлайн-прогон сезонности
даёт `Выкупы ТОП-3: 36361`, `growthYoY 1.79`, `growthFactor 3.2`, `used 36361`; сид-прогон
производства `циклов 20 срывов 0`. Красных нет. (Фикстура `scratchpad/srt.json` эфемерна: если
её нет в новой сессии — офлайн-прогон сезонности пропустить, попросить пользователя или пересоздать.)

## Открытые вопросы (нужно решение пользователя)
- **ЖДУ ОТВЕТА: финальное «поехали» на шаг 1 (движок круглогодичного режима).** План согласован
  (см. выше), детали одобрены. Осталось подтверждение начать код. Как получу — кодить движок,
  затем показать офлайн-результат (муслин=сезонный, пижама=круглогодичный) ДО правок UI.
- (не блокер) `growthMode` и потом `articleType` — пока переключатели в коде; вынести в UI Части 1
  вместе с работой над круглогодичным режимом.

## Следующий шаг
Дождаться «поехали» и реализовать **шаг 1** согласованного плана: в `planner/lib/season/forecast.js`
добавить `plan.articleType='allseason'` — годовое окно (365 дн), форма из полного рельефа без
обнуления, детектор мини-сезонов (+RU-календарь НГ/14фев/23фев/8мар), поддерживающий помесячный
подсорт с пред-настрелом под всплески. Проверить офлайн на фикстуре (муслин vs пижама), показать
пользователю цифры и форму, и только потом — отбор/якорь (шаг 2) и UI (шаг 3). Вопросы по развилкам
задавать ТЕКСТОМ, не через меню. Живые запросы к MPStats — только через бюджет приложения.
