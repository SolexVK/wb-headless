# Handoff — планировщик производства (planner/) + отчёты по рангу сезонности

Ветка: `claude/production-plan-twv8ki`. Дерево ЧИСТОЕ, последний коммит `b09e9a9` (docs: update handoff).
Пользователь: solexvk@gmail.com. Деплой планировщика — Mac mini (openclaw@Mac-mini-Vital),
launchd-ярлык `com.wbheadless.planner`, порт 8477.

В этой сессии было ДВА трека. Трек A (в коде, запушен) не трогали. Трек B (отчёты MPStats) — вся
работа была в **скретчпаде** и НЕ закоммичена (см. предупреждение ниже).

## Цель
- **Трек A — «Сокращение плана»** (в планировщике): срезать план ~342k→~200k шт, убирая слабые
  расцветки по выкупам. Готово и запушено; ждём, когда пользователь применит рез на боевых данных.
- **Трек B — аналитические PDF-отчёты (эта сессия):** по фото/ссылкам моделей из Google-таблиц
  строить (1) **ранг сезонности** ниши конкурентов через MPStats + инфографика; (2) **план пошива**
  (фото + размерная сетка + цвета×размеры×кол-во); (3) **динамику цены/остатков/заказов** и
  **оборачиваемость + экономику** (цена→маржа→прибыль, скорость прибыли ₽/день). Отдавать красивым PDF.

## АКТИВНАЯ ЗАДАЧА (5 сен) — анализ 8 брендов-конкурентов
По 8 брендам собрать ассортимент, найти лидеров по выручке и артикулы со взрывным ростом,
спроецировать на рынок, выдать шорт-лист «на что обратить внимание» → потом глубокий анализ.
Бренды: **MeetAIDA, TATIM, GOVORILI, Небесной красоты, LancetUniform, MAYRCHI, ФилаКалифа, Sitteria**.

Согласованные решения:
1. Лучшие продажи — по **выручке ₽** (primary) + показывать штуки.
2. Взрывной рост — **продажи 90 дн / предыдущие 90 дн ≥ 2×** + новинки (<6 мес) с быстрым набором.
3. Шорт-лист — **консолидация**: идентичные/повторяющиеся товары между брендами свести в единый
   ранжированный список по выручке; если товары различаются — **≥5 лучших на бренд**.
4. Формат v1 — **отдельный PDF на бренд + один вводный сводный PDF** по всем брендам (потом решим,
   сводить ли в один файл).
5. Окно — год + проверка сезонов на 2 годах; НО подстраиваться под дату старта продаж (новинки с
   короткой историей, но сильными результатами — рассматривать; сезоны по фактическому активному периоду).
6. Порог/сравнение — **адаптивный бенчмарк**: если артикул близок к топам ниши/группы → сравнивать с
   рынком; если далеко → сравнивать внутри его же бренда. Цель — показать ПОТЕНЦИАЛ, не только топ.
7. 4 сезона: весна мар–май · лето июн–авг · осень сен–ноя · зима дек–фев.

Подзадачи в трекере (этой сессии): П0 эндпоинт бренда+сверка названий; П1 сбор ассортимента;
П2 метрики (год+4 сезона+рост); П3 рыночная проекция; П4 консолидация+шорт-лист; П5 PDF.

**БЛОКЕР — токен:** старый `MPSTATS_TOKEN` в env исчерпал суточную квоту (429, use_wb_external 197/150).
Пользователь даёт токен ВТОРОГО аккаунта (лимиты выше), но НЕ через чат (чтобы можно было ротировать
без правок кода). Решение: значение задаётся как **секрет среды `MPSTATS_TOKEN`** в настройках среды
Claude Code (web) → **новая сессия** подхватывает его из env. Код читает `process.env.MPSTATS_TOKEN` по
имени — при ротации меняется только секрет, код не трогаем. Токен НИКОГДА не печатать/не коммитить.
Эндпоинт бренда: `POST https://mpstats.io/api/wb/get/brand?path=<бренд>&d1&d2` (bucket wb_external).

## Текущее состояние
**Трек A:** билд `plan-cut-sortcols-2026-08-31` (APP==BACKEND), коммит `109b34e`. Работают Фазы 0–3
(карта сшивки, выкупы 6 мес, разбор+симуляция реза, применение с откатом), CSV-экспорт, сворачивание
фаз, сортировка столбцов. НЕ применяли рез на Mac mini (ждём пользователя). Детали — в git-истории.

**Трек B (эта сессия):** сделаны и ОТПРАВЛЕНЫ пользователю PDF:
- Блузки (4 модели → 2 ниши): `rang-sezonnosti-v2.pdf` (инфографика) + `plan-poshiva.pdf`.
- Рубашки в клетку фланель (6 арт. AIDEMIKO): `rang-rubashki-kletka.pdf` (3 стр.: ранг+рекомендация
  «когда отшивать» / динамика рынка / оборачиваемость+экономика) + `plan-poshiva-rubashki.pdf`
  (обложка + 6 расцветок × размерная сетка XS–3XL, 500 шт/арт, всего 3000).
- Всё лежит в скретчпаде: `<scratchpad>/*.mjs` (генераторы) + `*.pdf` (результаты) + `shirts/`,
  `thumbs/`, `shirt-thumbs/` (фото), `*.json` (данные).

**Ключевые находки Трека B (бизнес):**
- Блузки нарядные (корсет/жабо/рюши): умеренная зимняя сезонность, пик декабрь, вход октябрь.
- Блузки атласные/шёлк: ВЫБИВАЮТСЯ — бимодально (июнь + декабрь), поэтому отдельная ниша.
- **Рубашки в клетку/фланель: контринтуитивно — ВЕСЕННИЙ товар** (пик март–апрель, провал окт–ноя),
  подтверждено на 1- и 2-летнем окне. Рекомендация: начинать пошив в октябре (ткань в сентябре),
  быть на WB к ~15 декабря → весенний пик 2027.
- Дорогой сегмент (2800–6000 ₽) держит цену крепче (3313–3970 ₽, −17%), max декабрь; масс-маркет
  демпингует летом (−20%).
- Оборачиваемость: сезон ~3.4 мес, межсезонье ~5.4 мес; скорость прибыли в сезон ×1.8 (≈7165 vs
  3901 ₽/день) при цикле ткань2н+пошив3н+логистика2н+продажа 1000 шт.

## ⚠️ ВАЖНО: работа Трека B в скретчпаде, эфемерна
Все `.mjs`-генераторы и данные — в session-скретчпаде, который стирается при пересоздании контейнера.
PDF уже у пользователя, но чтобы ПЕРЕСОБРАТЬ/повторить для новых моделей — скрипты нужно либо
закоммитить в репозиторий, либо воссоздать по описанию ниже. См. «Открытые вопросы».

## Файлы, над которыми работали
Код планировщика в этой сессии НЕ меняли. Задействован существующий движок:
- `planner/lib/season/seasonPlanReport.js` — `collectSerpAll({phrases,gender,priceMin,priceMax,
  minusWords,limit}, d1, d2, log)` тянет конкурентов по фразам (SERP MPStats, дневные ряды).
- `planner/lib/season/salesPlan.js` — `buildGroupDailySeries`, `buildSeasonPlan(groupDaily,{baseSource:
  'market',weekly:false})` → `{rank{rank,code,amplitude,p90,p50,p10}, phases{entry,ramp,peak,hotEnd,
  sale,monthlyProfile,stageOfMonth}, pricing{entry,peak,sale}, meanPrice, coeffs, dataSufficiency}`.
- `planner/lib/season/wbCard.js` — `fetchCardInfo(nmId)` (бесплатно, WB CDN).
- Скретчпад-генераторы (эфемерны): `shirt-analyze.mjs`, `dynamics.mjs`/`premium-dyn.mjs`,
  `shirt-season-build.mjs`, `dyn-charts.mjs`, `turnover-page.mjs`, `shirt-prodplan-build.mjs`,
  `season2-build.mjs`, `prodplan-build.mjs`, `analyze2.mjs`, `shirts2.mjs`.

## Что изменилось (по сути, Трек B)
- **MPStats работает прямо из контейнера:** `MPSTATS_TOKEN` есть в env (проверено:
  `GET https://mpstats.io/api/user/report_api_limit` c заголовком `X-Mpstats-TOKEN` → 200,
  лимит 150 запросов/сутки). Значит весь анализ можно гонять в облаке, не на Mac mini.
- **Данные:** SERP `/seo/keywords/{фраза}/serp-v2` (1 запрос/фраза, дневные ряды, цена, current
  balance; дневного остатка НЕТ). Дневной остаток/цена по товару — `GET /wb/get/item/{id}/sales?d1&d2`
  (365 строк: `data`,`sales`,`balance`,`final_price`,`discount`). Карточка+фото —
  `GET /wb/get/item/{id}` (`item.name/brand/final_price`, `photos[0].f` = full webp URL).
- **Ранг:** амплитуда p90/p50 коэффициента сезона (≥2.0 ярко / ≥1.3 умеренно / иначе слабо).
- **PDF:** HTML со встроенными data-URI картинками (webp/png в base64) + inline-SVG диаграммы →
  Chromium headless `--print-to-pdf`. Бинарь: `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`.
- **Упущенная выручка (OOS):** считаем сами по дневному остатку: дни с balance≤0 & sales≤0 внутри
  активного периода → восстанавливаем средние продажи «в наличии» × цену.
- **Фото из Google-таблицы:** экспорт `…/export?format=xlsx` → unzip → `xl/media/*` + привязка к
  строкам через `xl/drawings/drawing1.xml` (`<xdr:from><xdr:col>/<xdr:row>`). Колонка A (col=0) на
  строке-якоре блока модели = фото нашего изделия.

## Что пробовали и НЕ сработало (грабли Трека B)
- **Google Sheet CSV-экспорт по gid других листов → 0 байт** (пустые листы) и **htmlview прячет
  URL-гиперссылки** (рендерит JS-приложение). Рабочий путь: `export?format=csv&gid=0` (с `-L`!) для
  первого листа + `export?format=xlsx` для встроенных фото. `gviz/tq?tqx=out:csv` тоже отдал 0 по
  пустым листам.
- **`export?format=csv` без `-L` → HTTP 307, пустой файл.** Нужно следовать редиректу.
- **WB card CDN (`wbbasket.ru`) для 2 из 5 nmID → «карточка не найдена»** (перебор basket-хостов не
  попал в диапазон). Фикс: фото и цену брать через MPStats `/wb/get/item/{id}` (`photos[0].f`,
  `final_price`) — надёжнее и без гадания о хосте.
- **`identify`/`montage`/`convert` (ImageMagick) НЕТ, `PIL` НЕТ** в контейнере. Коллаж/кроп фото —
  через Chromium: HTML с картинками → `--screenshot`. Размеры картинок — через `file`.
- **SendUserFile упал на «could not be parsed as JSON»** — из-за кириллического пути в JSON и того,
  что `files` — массив. Фикс: имя файла в ASCII, `files:["/path.pdf"]` строго массивом.
- **Демпинг из SERP не считался** — в `dailyFull` `balance=0`. Пришлось тянуть дневные остатки
  из `/wb/get/item/{id}/sales`.
- **Первый график цены был «средним по рынку»** (дешёвые объёмные продавцы тянут вниз). Пользователь
  попросил дорогой сегмент → пересобрали цену по конкурентам 2800–6000 ₽ (с минус-словами
  пижама/костюм/брюки/штаны/комплект, иначе лезут пижамы-комплекты).
- **`node` из скретчпада не видит `./lib/...`** (cwd — скретчпад). Импортировать движок по
  АБСОЛЮТНОМУ пути `/home/user/wb-headless/planner/lib/season/...`.
- **`kill`/`pkill` в составной bash-команде → exit 144** (сигнал шеллу). Фон-процессы (Chromium)
  завершаются сами; отдельный `pgrep -af` для проверки.
- **1-летнее окно = один календарный год «folded»** (каждый месяц 1 раз) — уязвимо к разовым
  всплескам. Всегда перепроверять сезонность на 2-летнем окне (`d1` = −2 года).

## Команды для проверки
Проверка токена MPStats (должно быть HTTP 200 + лимит):
```
curl -s -H "X-Mpstats-TOKEN: $MPSTATS_TOKEN" https://mpstats.io/api/user/report_api_limit
```
Прогон анализа ниши (пример, из planner/, движок по абсолютному пути в .mjs):
```
cd /home/user/wb-headless/planner && node <scratchpad>/shirt-analyze.mjs   # → shirt-analysis.json
```
Сборка PDF из HTML:
```
/opt/pw-browsers/chromium-1194/chrome-linux/chrome --headless --no-sandbox --disable-gpu \
  --no-pdf-header-footer --print-to-pdf=out.pdf "file://$PWD/report.html"
```
Планировщик (Трек A), синтаксис в planner/:
```
node --check server.js && node --check public/app.js && node --check lib/planCut.js
```
**Ожидаемое зелёное сейчас:** git-дерево чистое, ветка на `b09e9a9`; MPStats-токен рабочий
(лимит 150/сутки, за сессию потрачено ~90–110); PDF-отчёты собраны и отправлены. Формальных тестов
нет — «зелёное» = токен отвечает 200, `node --check` ок, Chromium генерит валидный PDF (3 стр. для
рубашек, `file *.pdf` → «PDF document»). КРАСНОЕ по определению: скретчпад-скрипты исчезнут при
пересоздании контейнера (не закоммичены).

## Открытые вопросы (нужно решение пользователя)
- **Закоммитить ли генераторы отчётов в репозиторий** (напр. `planner/tools/season-report/`), чтобы
  не потерять пайплайн и переиспользовать для новых моделей? Рекомендую ДА (сейчас всё эфемерно в
  скретчпаде). ЖДУ ОТВЕТА. Альтернатива — встроить в планировщик как кнопку «Сводный ранг → PDF»
  (пользователь эту идею упоминал).
- **Точные сроки пошива** в модели оборачиваемости — оценка (цикл 49 дн + продажа калибрована на
  45–60 дн в сезон). Можно заменить на реальную мощность цехов из планировщика. ЖДУ, нужна ли эта
  интеграция или оценки достаточно.
- **Образец 4 (атласная блузка):** фото — низкоресное видео-превью; в матрице упомянут бежевый цвет,
  не заведён. ЖДУ нормальное фото + подтверждение цветов.
- **Трек A:** применять ли рез сейчас и на какой метрике — ЖДУ (пользователь пока переключился на
  отчёты).

## Следующий шаг
**Активно: анализ 8 брендов (см. секцию «АКТИВНАЯ ЗАДАЧА» выше).** Порядок:
1. Убедиться, что новый `MPSTATS_TOKEN` (из секрета среды 2-го аккаунта) подхватился: проверить
   `echo ${#MPSTATS_TOKEN}` (только длина, НЕ значение) + `curl -s -H "X-Mpstats-TOKEN: $MPSTATS_TOKEN"
   https://mpstats.io/api/user/report_api_limit` (должно быть больше 150 и не 429).
2. П0: `POST /wb/get/brand?path=<бренд>&d1&d2` на одном бренде (Sitteria) — подтвердить поля/дневные
   графики; сверить точные написания всех 8 брендов на WB (некоторые могут отличаться).
3. П1→П5 по плану. Переиспользовать `buildSeasonPlan` для сезонности предметов, Chromium для PDF.
Прошлый трек отчётов (блузки/рубашки) — сделан и отправлен; генераторы в скретчпаде (эфемерны, вопрос
о коммите в репо отложен пользователем: сначала бренды).
