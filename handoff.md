# Handoff — инструменты для Wildberries

Ветка: **`claude/wildberries-tools`**. Продолжаем разработку инструментов здесь.
(Ветка `claude/wildberries-api-docs-bfnvun` — отдельная, там только правила WB API.)

## Цель
Каскад инструментов для WB: ТОП-10 по запросу → фильтр → «Сравнение карточек» →
«Конкурентный анализ». Инструменты сцепляются через общий JSON-контракт (ключ `nmId`).
Архитектура и контракт — в `docs/pipeline/README.md`.

## Текущее состояние (что работает)
- **Правила/база WB API** (перешли из соседней ветки): `lib/wbClient.js` (лимиты,
  token-bucket, X-Ratelimit, ретраи), `lib/wbToken.js` + `npm run token:check`,
  снимки доки `docs/wb-api/*`, скилл `wb-docs`. Токен WB — рабочий (env `Wildberries_API`).
- **Джем-отчёты (API)**: `lib/wbJemReports.js` + `scripts/wb-jem-report.mjs`
  (`npm run cards:compare` не путать — это `jem`: create→status→getFile, лимиты зашиты).
- **Вход в кабинет (браузер)**: `lib/wbCabinet.js` — куки + localStorage-токен через
  Playwright storageState, обход антибота + TLS1.2 под прокси. Проверка сессии:
  `npm run cabinet:check`. Разведка страниц: `scripts/wb-cabinet-open.mjs`.
- **«Сравнение карточек» (готово, проверено на аккаунте)**: `lib/wbCardsCompare.js`
  + `scripts/wb-cards-compare.mjs`. DRY-RUN (без траты лимита) / `--submit` (тратит 1)
  / `--export-existing` (пере-скачать готовое бесплатно). Выгрузка XLSX
  автоматизирована (Создать Excel→Сформировать→Загрузки→скачать→развернуть ZIP).
- **Экстрактор данных**: `lib/wbCardsCompareParse.js` — XLSX → нормализованный JSON
  (метрики воронки по nmId: CTR, конв. в корзину/заказ, % выкупа, показы, заказы…).
  `runCardsComparison` сам пишет `*.json` рядом с `*.xlsx`.
- Скилл `wb-cards-compare`.
- **[1] ТОП по ключевой фразе (MPStats) — ПЕРЕРАБОТАН ПОД ФИНАЛЬНУЮ СПЕКУ, ПРОВЕРЕН
  ЖИВЫМ ТОКЕНОМ** — `lib/wbTopKeywords.js` + `scripts/wb-top-keywords.mjs`
  (`npm run top:keywords`). **Эндпоинт залочен**: `POST https://mpstats.io/api/
  analytics/v1/wb/search/items` (versioned API, Laravel 422). Ключевое слово —
  query-параметр **`path`**; период `d1`/`d2` (d2 строго РАНЬШЕ сегодня); тело
  ag-grid `{startRow,endRow}`; ответ `{total,data:[{position,id,...}]}`, `id`=nmId.
  `final_price_average` = **средняя цена продажи** (= revenue/sales, проверено).
  Старый `/wb/get/search` — html-заглушка, тупик.
  **Конвейер (согласован и обкатан на живых прогонах):** тянем ВСЮ выдачу за период
  (пагинация; ПОТОЛОК MPStats ~100 товаров/фразу — больше эндпоинт не отдаёт) → отсев
  выручки `<revenueFloor` (дефолт 100k) → фильтры по НАЗВАНИЮ: **exclude — жёсткий гейт**
  (любое слово → вон; сюда и противоположные значения, и «не наши» категории), **группы
  соединяются ИЛИ** (матч хотя бы по одной = метка, не жёсткий И — иначе теряются лидеры
  с частичным описанием) + **исключение для «безхвостых»** (генерик без ключей групп
  остаётся, если выручка ≥ ТОП-20 пула; порог считаем ПО ПУЛУ БЕЗ exclude-хитов) →
  метрические фильтры (коридор по средней цене продажи, опц. рейтинг/отзывы/продажи) →
  сортировка по выручке → топ-N ПОСЛЕДНИМ шагом. Матчинг подстрокой — ключи задаём
  СТЕМАМИ (`прям/приталенн/стойк/без ворот`). `lostProfit` — доп. показатель, не фильтруем.
  `matchType`: `group`/`exception`/`all`. Контракт top-rivals — см. `docs/pipeline/README.md`.
  Чистая логика проверена на фикстуре; живьём обкатано на «Рубашка женская муслиновая»
  (ИЛИ вернул лидеров-«оверсайз», строгий И их терял — 48 карточек против 2).
  CLI: `--query --period N|--d1/--d2 --top --group "крой=оверс,прям" (повторяемый)
  --exclude "притал,стойк,без ворот,волан,парео" --price-min/max --min-rating/... --our
  --nmids-only | --out f.json | --html f.html`. Порядок полей: nmId → бренд → название →
  метрики. **Осознанное ограничение:** топ-500 через этот эндпоинт недостижим (потолок 100).
  **HTML-отчёт** (`--html`): фото карточки (вшито в data-URI) + цвет, кликабельные
  артикулы, бейджи типа. Имя файла отчёта авто = `<фраза>_<дата>.html` (легко искать).
  **Зафиксирован как СКИЛЛ** `.claude/skills/wb-top-keywords/` — работает и отдельно,
  и в каскаде. Обкатан на двух фразах («муслиновая» 48, «в полоску» 56) — зелено.
  **Предзапрос (вариант A):** параметры собираем в диалоге в чате (фраза→интервал→топ→
  фильтры), показываем итог, пользователь подтверждает — запускаем (под капотом флаги CLI).
- **[3] Конкурентный анализ (готово, проверено на фикстурах)** — `lib/wbCompetitiveAnalysis.js`
  + `scripts/wb-competitive-analysis.mjs` (`npm run analysis:competitive`). Вход —
  контракт `cards-compare.json` из [2]. Сравнивает нашу карточку с конкурентами по
  воронке (CTR→корзина→заказ→выкуп, ищет «бутылочное горлышко»), цене (премиум/средний/
  бюджет), качеству (рейтинг/отзывы), позиции в поиске, тренду период-к-периоду. Отдаёт
  контракт `competitive-analysis` (JSON, флаг `--json`) + markdown-отчёт (`formatReport`).
  Чистая логика; проверена на 2 фикстурах (проседающий и лидирующий кейсы) — зелено.
- **Оркестратор каскада (готово)** — `scripts/wb-pipeline.mjs` (`npm run pipeline`).
  Сцепляет [1]→[2]→[3] одной командой; артефакты в `reports-output/<run-id>/`
  (`top-rivals.json`, `cards-compare.*`, `analysis.json`, `analysis.md`). Без `--submit` —
  DRY-RUN ([1] + план [2], лимит не тратим). `--submit` = полный каскад (тратит 1 лимит,
  нужна живая сессия кабинета). `--export-existing` = [2 готовое]→[3] бесплатно.
  DRY-RUN проверен вживую ([1]→план, пишет top-rivals.json).
  **NB:** вызов [1] внутри оркестратора написан ДО переработки [1] — старая форма
  `filters` (min-revenue/sort) там ещё не заменена на новую (deep-группы/период/пресеты).
  Не ломается (метрические фильтры работают), но глубокие фильтры и период туда не
  прокинуты — доделать при возврате к каскаду.

## Что дальше (следующий шаг)
Инструмент [1] доведён до согласованной спеки. По плану пользователя: сверять каскад
ПО ШАГАМ, [1] был первым. Дальше — по решению пользователя двигаться к следующим
инструментам каскада (у него своё видение схемы работы — сперва проговорить, потом
кодить). Технический хвост: обновить вызов [1] в оркестраторе под новую форму `filters`
(deep-группы + период/пресеты) — но только когда дойдём до каскада.

### (старое) Что дальше — до сверки по шагам
Каскад [1]→[2]→[3] собран и связан. Осталось единственное, что нельзя сделать без
пользователя/траты лимита: **прогнать полный каскад вживую с `--submit`** на реальном
нашем артикуле — это подтвердит связку [2]→[3] на настоящих данных кабинета (стоит 1
из лимита «Сравнения карточек» и требует живой сессии кабинета; env WB_COOKIES/
WB_LOCALSTORAGE в среде есть — проверить `npm run cabinet:check`). Спросить у
пользователя наш артикул и разрешение потратить лимит. Альтернатива без траты —
`--export-existing`, если в истории кабинета уже есть готовое сравнение.
Идеи на потом: экспорт отчёта [3] в XLSX/PDF, доп. источники данных для анализа.

## Важно про секреты (иначе кабинет не откроется)
Куки/токен кабинета живут в `.secrets/` (gitignored) и **в новую сессию НЕ переносятся**.
В начале работы проверить: `npm run cabinet:check`. Если «не авторизованы» — попросить
у пользователя свежие куки (Cookie-Editor→Export JSON) и localStorage-токен
(`wb-eu-passport-v2.access-token` и др.), положить в `.secrets/wb-cookies.json` и
`.secrets/wb-localstorage.txt`, ИЛИ задать env `WB_COOKIES` / `WB_LOCALSTORAGE`.
Токен WB API (для Джем-отчётов) — env `Wildberries_API` (может уже быть в среде).
Токен MPStats (для [1] ТОП-N) — env `MPSTATS_TOKEN` (заголовок `X-Mpstats-TOKEN`).
Проверить: `[ -n "$MPSTATS_TOKEN" ]`. Если пусто — добавить в переменные окружения среды.

## Команды для проверки
```bash
node --check lib/*.js scripts/*.mjs      # синтаксис
npm run token:check -- --ping            # токен WB API жив? (ожидаем HTTP 200)
npm run cabinet:check                    # сессия кабинета жива? (нужны .secrets или env)
npm run cards:compare -- --our <nm> --rivals <nm,nm>   # DRY-RUN (без траты лимита)
npm run top:keywords -- --query "платье женское" --top 10   # [1] — нужен MPSTATS_TOKEN
npm run pipeline -- --query "платье женское" --our <nm> --top 4   # каскад DRY-RUN (лимит не тратит)
# [3] проверяется на своём JSON: node scripts/wb-competitive-analysis.mjs --in <cards-compare.json>
```
Ожидаемое «зелёное»: синтаксис чист; `token:check --ping` = 200 если задан `Wildberries_API`;
`cabinet:check` = «АВТОРИЗОВАНЫ» только при наличии свежих кред (иначе честно скажет обновить);
`top:keywords` = JSON `top-rivals` если задан `MPSTATS_TOKEN` (иначе честно попросит токен).
NB: свежий контейнер — сначала `npm install` (иначе `cabinet:check` упадёт без playwright).

## Открытые вопросы (нужно решение пользователя)
Блокеров нет. [1] ТОП-N готов и залочен (`MPSTATS_TOKEN` в среде подхватился, эндпоинт
подтверждён живьём). Двигаемся к [3] Конкурентный анализ + оркестратор.
