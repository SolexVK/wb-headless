{
  "name": "wb-headless",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=18.18.0 <21"
  },
  "scripts": {
    "start": "node server.js",
    "build:chrome": "npx puppeteer browsers install chrome --force --path=/opt/render/.cache/puppeteer",
    "postinstall": "npm run build:chrome"
  },
  "dependencies": {
    "express": "^4.19.2",
    "puppeteer": "^22.10.0",
    "uuid": "^9.0.1"
  }
}
