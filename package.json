{
  "name": "wb-headless",
  "version": "1.0.1",
  "type": "module",
  "engines": { "node": ">=20.x" },
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx puppeteer browsers install chrome --cache-dir=/opt/render/.cache/puppeteer"
  },
  "dependencies": {
    "express": "^4.19.2",
    "puppeteer": "^22.10.0",
    "uuid": "^9.0.1"
  }
}
