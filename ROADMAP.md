# План доработок Telegram-бота (Telegram ↔ Claude Agent)

Статус: `[ ]` — не сделано, `[x]` — сделано.

## Ключевая архитектурная проблема
Раньше выполнение задачи блокировало цикл опроса Telegram — пока агент работал,
бот не принимал сообщения (не работали `/stop`, статус, очередь). Исправляется в
Tier 0 (0.1): опрос и выполнение расцеплены — независимые циклы `poll` и `worker`.

## Tier 0 — Надёжность
- [x] 0.1 Расцепить polling и выполнение (очередь задач + отдельный воркер)
- [x] 0.2 Автозагрузка `.env` (`node --env-file`)
- [x] 0.3 Таймаут на задачу (`AbortController` + `AGENT_TIMEOUT_MS`)
- [x] 0.4 Graceful shutdown (`SIGINT`/`SIGTERM`, дождаться/прервать задачу, сохранить состояние)
- [x] 0.5 Персистентность сессии (`.bot-state.json`, чтение при старте)
- [x] 0.6 Обработка 409 Conflict (два экземпляра бота)

## Tier 1 — Контроль и обратная связь (UX)
- [x] 1.1 Прогресс в реальном времени (события `tool_use` → строки `🔧 …`)
- [x] 1.2 `/stop` — отмена текущей задачи + очистка очереди
- [x] 1.3 `/status` — занят/очередь, модель, режим, папка, id сессии, потрачено $
- [x] 1.4 HTML-форматирование ответов (код/жирный) с откатом в простой текст
- [x] 1.5 Лимит стоимости в день (`DAILY_COST_LIMIT`, из `total_cost_usd`)
- [x] 1.6 `/model`, `/mode` — переключение модели и режима прав из чата

## Tier 2 — Мощные фичи
- [ ] 2.1 Подтверждение опасных операций в чате (`canUseTool` + инлайн-кнопки)
- [ ] 2.2 Git-шорткаты (`/diff`, `/commit`, `/push`)
- [ ] 2.3 Приём и отдача файлов (Telegram `document` ↔ `BOT_WORKDIR`)
- [ ] 2.4 Несколько проектов (`/project <name>` → смена `cwd`)
- [ ] 2.5 Несколько пользователей (`TELEGRAM_ALLOWED_CHATS` — список)
- [ ] 2.6 Голосовые → текст (транскрипция `voice`)

## Tier 3 — Эксплуатация и деплой
- [ ] 3.1 Always-on (`systemd` / `pm2` / Docker + автоперезапуск)
- [ ] 3.2 Режим webhook (`/telegram/webhook` в `server.js` + `setWebhook`)
- [ ] 3.3 Логирование в файл (структурные логи, ротация)
- [ ] 3.4 Тесты (чанкинг, проверка chat id, парсинг команд; mock-режим агента)

## Рекомендуемый порядок
1. Весь Tier 0 (фундамент, особенно 0.1).
2. Из Tier 1: 1.1 + 1.2 + 1.5 (прогресс, отмена, лимит $).
3. Дальше по потребности; 2.1 — самое ценное для безопасности.

Минимальный «боевой» первый спринт: 0.1–0.6 + 1.1 + 1.2 + 1.5.
