---
name: resume
description: Возобновить работу из handoff.md — прочитать состояние, проверить, что сборка/тесты рабочие, задать открытые вопросы и продолжить со «Следующего шага». Использовать в начале сессии. Триггеры — «/resume», «продолжаем», «возобнови работу».
---

# /resume — поднять контекст и продолжить

Прочитай файл `handoff.md` в корне репозитория целиком. Если файла нет — скажи об этом и предложи
осмотреться (git log, README) вручную. Затем:

0. **Сверься с канонической веткой (ПЕРВЫМ делом).** handoff.md вверху указывает
   каноническую ветку (строка «Ветка: …»). Веб-сессия часто стартует на авто-ветке,
   сгенерированной из имени чата (напр. `claude/resume-*`), — это НЕ та ветка.
   Это стоячее указание пользователя: работать всегда в канонической ветке, а не в
   авто-ветке чата. Поэтому:
   - `git branch --show-current`. Если совпадает с канонической — ок, идём дальше.
   - Если НЕ совпадает и на текущей ветке ещё нет твоих коммитов этой сессии —
     переключись на каноническую: `git fetch origin <канон> && git checkout <канон>`.
   - Если ты уже успел закоммитить в авто-ветку — не теряй работу: перенеси её в
     каноническую fast-forward’ом (`git checkout <канон> && git merge --ff-only <авто-ветка>`),
     затем продолжай в канонической. Дальше все коммиты и пуши — только в неё.
   - Кратко сообщи пользователю, если пришлось переключаться.

1. **Проверь состояние.** Прогони команды из раздела «Команды для проверки» и **сверь результат с
   ожидаемым состоянием**, описанным в файле.
   - Всё сходится (зелёное там, где ожидалось зелёным; известные красные — те самые) → фундамент
     надёжен, двигаемся дальше.
   - Расхождение (сломалось что-то новое) → **сначала почини это**, не строй новое поверх сломанного;
     сообщи пользователю, что именно разошлось.
2. **Открытые вопросы.** Если в разделе «Открытые вопросы» есть развилки, требующие решения
   пользователя — **задай их ПРЕЖДЕ чем продолжать** работу.
3. **Продолжай.** Если проверки зелёные и блокеров нет — приступай к разделу «Следующий шаг».

Кратко отчитайся пользователю: что показали проверки (сошлось/нет), какие открытые вопросы висят,
и с чего начинаешь.
