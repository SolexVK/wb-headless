# Анализ ниши (категории Wildberries)

Отчёт оценивает нишу (категорию WB) перед выходом в неё: насколько она
ёмкая, «живая» и не монополизирована ли крупными игроками. Данные берутся
из MPSTATS (отчёт по категории, `POST /wb/get/category`).

## Что показывает

Из выгрузки товаров категории за период считаются агрегаты:

| Метрика | Что означает |
|---|---|
| **Товаров в нише** | Сколько всего карточек в категории (по данным API). Если выгрузили не всё — анализируем верх по выручке |
| **Продавцов / брендов** | Сколько разных продавцов и брендов делят нишу (конкуренция) |
| **С продажами, %** | Доля карточек с ненулевыми продажами — «живой» спрос против мёртвых листингов |
| **Выручка ниши** | Суммарная выручка и штуки за период — ёмкость |
| **Цена сред./медиана** | Ценовой диапазон ниши (медиана устойчивее к выбросам) |
| **Выручка на карточку** | Средняя выручка на товар (и отдельно на «живой» товар с продажами) |
| **Монополизация** | Доля выручки топ-10 товаров — высокая = новичку тяжело пробиться |
| **Лидер** | Топ-продавец и его доля выручки ниши |
| **Рейтинг / отзывы** | Средние по нише — косвенно про «зрелость» карточек |

Плюс разрезы: **топ-продавцы** и **топ-бренды** ниши (выручка, число
карточек, доля). Товары в CSV отсортированы по убыванию выручки.

## Методология

1. Товары категории тянем постранично (`POST /wb/get/category`,
   сортировка по выручке ↓). Если ниша больше `NICHE_MAX_ROWS`, берём
   верх по выручке — самые значимые игроки.
2. **С продажами** = карточки, где штуки или выручка > 0.
3. **Средняя цена** и **медиана** считаются только по карточкам с ценой > 0.
4. **Монополизация** = выручка топ-10 товаров ÷ выручка ниши.
5. **Доля продавца/бренда** = его выручка ÷ выручка ниши.

## Переменные окружения

| Переменная | Обязательна | По умолчанию | Назначение |
|---|---|---|---|
| `MPSTATS_TOKEN` | да | — | Токен MPSTATS |
| `NICHE_PATH` | да* | — | Путь категории WB (`Женщинам/Одежда/Платья`). *Можно передать аргументом или `?path=` |
| `REPORT_DAYS` | нет | `30` | Длина периода по умолчанию (дней) |
| `NICHE_MAX_ROWS` | нет | `5000` | Сколько товаров максимум выгрузить |
| `NICHE_PAGE_SIZE` | нет | `500` | Размер страницы выгрузки |

## Запуск

### 1. Из командной строки

```bash
# период = последние REPORT_DAYS дней
MPSTATS_TOKEN=xxx NICHE_PATH="Женщинам/Одежда/Платья" npm run report:niche

# путь и явный период аргументами
MPSTATS_TOKEN=xxx node report-niche.js "Женщинам/Одежда/Платья" 2026-06-01 2026-06-30
```

Результат — `reports-output/niche-<категория>-<d1>_<d2>.csv` и `.json`
плюс сводка в консоль. CSV в UTF-8 с BOM и разделителем `;` — открывается
в Excel без плясок.

### 2. По HTTP (сервис запущен)

```bash
# JSON
curl -H "x-api-key: $API_KEY" \
  "http://localhost:8080/reports/niche?path=Женщинам/Одежда/Платья&d1=2026-06-01&d2=2026-06-30"

# CSV-файл
curl -H "x-api-key: $API_KEY" \
  "http://localhost:8080/reports/niche?path=Женщинам/Одежда/Платья&format=csv" -o niche.csv
```

Без `d1`/`d2` берётся период = последние `REPORT_DAYS` дней. Параметр
`path` обязателен.

### 3. Через GitHub Actions

Вкладка **Actions** → workflow «Анализ ниши (категории WB)» → **Run
workflow**. Заполните поле **path** (путь категории), при желании период
и `max_rows`. Готовый CSV+JSON приложится к запуску как артефакт
`niche-report`. Нужен секрет репозитория `MPSTATS_TOKEN`.

## Колонки CSV (товары)

| Поле | Описание |
|---|---|
| `SKU` | Артикул WB (nmId) |
| `Название` | Название товара |
| `Бренд` | Бренд |
| `Продавец` | Продавец |
| `Цена, ₽` | Актуальная цена |
| `Продано, шт` | Продано за период |
| `Выручка, ₽` | Выручка за период |
| `Остаток, шт` | Текущий остаток |
| `Рейтинг` | Рейтинг карточки |
| `Отзывов` | Число отзывов |

Полные агрегаты и разрезы по продавцам/брендам — в `.json`.
